version: "3.8"

services:
  phone-bot:
    build: .
    container_name: phone-assistant
    ports:
      - "17293:8000"
    env_file:
      - stack.env
    restart: unless-stopped
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
